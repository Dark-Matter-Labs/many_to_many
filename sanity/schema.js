import alert from './schemas/alert';
import deep_code_shift from './schemas/deep_code_shift';
import insight from './schemas/insight';
import layer from './schemas/layer';
import legal_theme from './schemas/legal_theme';
import story from './schemas/story';
import system_blocker from './schemas/system_blocker';
import tool from './schemas/tool';

export const schema = {
  types: [
    alert,
    deep_code_shift,
    insight,
    layer,
    legal_theme,
    story,
    system_blocker,
    tool,
  ],
};
